const FRAGMENT = 'application/x-dtable-fragment';
const HTML = 'text/html';
const TEXT = 'text/plain';
const FILES = 'files';
const DTABLE_FRAGMENT = 'dtable-fragment';

const transferTypes = {
  FRAGMENT,
  HTML,
  TEXT,
  FILES,
  DTABLE_FRAGMENT,
};

export default transferTypes;
